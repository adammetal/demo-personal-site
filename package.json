{
  "name": "hello-web",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "parcel build './src/**/*.html' --no-cache --public-url /demo-personal-site",
    "start": "parcel serve './src/**/*.html' --open",
    "deploy": "gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gh-pages": "^6.1.1",
    "parcel": "^2.11.0"
  }
}
